# 交接文档 · daily-review（A股每日复盘）

> 交接日期：2026-09-09

## 1. 项目定位

存放每日 A 股收盘复盘报告（自包含单文件 HTML），当前产出为**技术面三维复盘**（路径一：风险面看上证 / 进攻面看国证2000 / 情绪面看全市场广度）。可扩展情绪周期、短线博弈、全景复盘等其他路径。

## 2. 目录与文件

```
daily-review/
├── 技术面复盘_20260909.html   # 首份报告（表头已修复：「20日外口径说明」→「均线/形态备注」）
├── tools/check.js            # 自检脚本（结构/JS语法/图表/表头占位检查）
├── .gitignore                # 拦截 token/secret/key/env 等敏感文件
├── 交接文档.md               # 本文件
└── .git/                     # 独立 git 仓库（repo 内身份：VingerFPS / vinger@local）
```

上级目录另有 `daily-review-backup.bundle`（完整 git bundle，OneDrive 同步，脱敏后重建）。

## 3. 数据采集（关键依赖）

- **数据 CLI**：`westock`（腾讯自选股数据接口），已安装于 `<user-home>\.local\bin\westock.exe`
- **PATH**：bash 会话需先 `export PATH="<user-home>/.local/bin:$PATH"`（未写入 ~/.bashrc，如丢失按 westock-data skill 的 `scripts/setup.cjs` 重装）
- **常用命令**：
  - 指数行情：`westock quote sh000001,sz399001,sz399006,sz399303,bj899050`
  - 涨跌分布（涨停/跌停首选口径）：`westock changedist`
  - 市场总览/量能：`westock market-overview --type trade,rotation`
  - K线：`westock kline sh000001 --period day|week --limit 25`
  - 技术指标（MA/MACD/KDJ/BOLL/RSI）：`westock technical sh000001,sz399303`
  - 板块排行：`westock sector ranking`；热点：`westock hot sector`
  - 连板天梯：`westock screen ranking --type limitup_days --limit 20`
- **纪律**：所有行情数据只走 westock CLI，禁止 curl / WebSearch 替代；数据为空时如实标注，不编造。

## 4. 报告生成流程（可复现步骤）

1. 判断目标日期是否为交易日（周末/节假日 → 「今日休市」，终止）。
2. 采集上述数据（quote / changedist / market-overview / 两个指数的日K周K / technical / sector ranking / hot sector / 连板天梯）。
3. 按 HTML 模板结构写作：`header → .tldr 首屏结论卡 → 指数全景 → ①风险面 → ②进攻面 → ③情绪面 → ④主线板块（P0-P3分级） → ⑤三档建仓（条件→操作→止损） → ⑥次日观察 → ⑦风险提示 → footer（免责声明+统计日期）`。
4. 图表用**内联 SVG + 内联 JS**（不用 ECharts CDN），保证离线可打开；涨红跌绿、¥/亿单位。
5. 自检：`node tools/check.js 技术面复盘_YYYYMMDD.html`（检查结构、内联 JS 语法、图表空壳+填充引用、表头措辞、文件名与日期一致性），通过后再交付；UI 变更另需真实浏览器验证：桌面宽度 + 390px 窄屏各查一次——图表能显示、无文字重叠、`document.documentElement.scrollWidth ≤ innerWidth`（无横向溢出）。
6. 命名 `技术面复盘_YYYYMMDD.html`，`git add -A && git commit`。

## 5. 约定与红线

- 上涨红（#dc2626）/ 下跌绿（#16a34a），浅底深字研报风。
- 不给「建议买入/卖出」，只用「条件 → 操作 → 止损」框架；结尾附免责声明。
- 措辞中性（「综合盘面表现」「资金验证」），不暴露具体数据源名称。
- 结论可追溯：关键数字标注数据时点。

## 6. 已知边界 / 待办

- [x] ~~远端仓库~~ 已推送 GitHub：`origin = https://github.com/Vinger-lee/daily-reviwe.git`（main 分支，2026-09-09）。注意：历史已经 filter-branch 脱敏改写并 force push（内网 IP/主机别名/密钥名/本机路径→占位符），旧 SHA 全部失效勿引用；`.gitignore` 拦截敏感文件。令牌未落盘，后续推送需自备凭据。另保留 `../daily-review-backup.bundle`（脱敏后重建）。
- [x] ~~表头措辞错误~~ 已修复：「20日外口径说明」→「均线/形态备注」（2026-09-09）。
- [x] ~~浏览器渲染验证~~ 已验证（2026-09-09：Chromium 实测 #chartSH/#chartGZ 均填充 ~6.3KB SVG、共 167 个图元，截图确认日K折线+红绿量柱正常渲染）。
- [x] ~~隐私加固~~ 已完成（2026-09-09：全历史敏感词扫描 0 命中，含 token/内网 IP/本机路径/密钥名）。
- [ ] 三档建仓价位基于当日指标静态推算，每次生成需重算。
- [ ] 如需情绪周期/短线博弈等其他路径报告，参照 A股每日复盘 skill 的路径二～四扩展章节结构。
- [ ] westock CLI 依赖网络；离线环境下只能查看历史报告，无法取新数据。
- [ ] OneDrive 目录坑：本仓库位于 OneDrive 同步目录，git 对 loose ref 的原子写（lockfile+rename）会静默丢失（症状：`git fetch` 后 `origin/main` 立即变 `[gone]`）。已验证的解法：`mkdir -p .git/refs/remotes/origin && git rev-parse HEAD > .git/refs/remotes/origin/main && git pack-refs --all`——打包进 `packed-refs` 单文件后可长期存活。若未来再现 `[gone]`，重跑该命令即可；根治需把仓库迁出 OneDrive 同步目录。
